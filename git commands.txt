Download : git clone "url"
Check current branch : git branch *branch(green)
Check local changes : git status
Stage change : git add .
Commit : git commit -m "commit message"
Push(Upload) : git push origin "branch name"
Pull(Update) : git pull origin "branch name"

Changes cycle

change(red) --> stage(green) --> commit --> push

Hiiiiii
